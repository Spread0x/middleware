import test from 'ava';

test('Array #indexOf() should return -1 when the value is not present', t => {
  //it('should return -1 when the value is not present', function() {
      t.is([1,2,3].indexOf(4), -1);
   //});
});


